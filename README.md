# CAI OSINT Recon Agent

The project's goal is to build a constrained CAI-based reconnaissance AI agent that performs authorised OSINT and attack-surface discovery against approved targets only. It only works on targets that you have permission to test. It automatically combines the results into a final security report.

The main demo target is:

```text
scanme.nmap.org